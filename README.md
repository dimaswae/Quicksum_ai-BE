#Quicksum_ai-BE
Masukkan API KEY di file .env lalu  
```pip install python-dotenv```  
Setelah itu install Requirements  
```pip install -r requirements.txt```  
Jalankan program  
```python run.py```  
Akses melalui  
```http://localhost:8000/docs#/default/summarize_api_summarize_post```
